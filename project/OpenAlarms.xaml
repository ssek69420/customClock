<Page x:Class="project.OpenAlarms"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:project"
      mc:Ignorable="d">

    <Grid>

        <!-- Background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="BlueViolet" Offset="0"/>
                <GradientStop Color="AliceBlue" Offset="0.55"/>
                <GradientStop Color="DarkBlue" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>


        <!-- Main Content -->
        <Grid Width="900"
              HorizontalAlignment="Center"
              VerticalAlignment="Center">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="440"/>
            </Grid.ColumnDefinitions>


            <!-- ========================= -->
            <!-- CREATE ALARM -->
            <!-- ========================= -->

            <Border Grid.Column="0"
                    Padding="28"
                    CornerRadius="28"
                    Background="#22000000"
                    BorderBrush="Pink"
                    BorderThickness="1">

                <StackPanel>

                    <!-- Title -->
                    <TextBlock Text="Set Alarm"
                               FontSize="28"
                               FontWeight="SemiBold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"/>


                    <!-- Time Input -->
                    <TextBox x:Name="TimeTextBox"
                             Width="200"
                             Height="50"
                             Background="#22000000"
                             BorderBrush="#66FFFFFF"
                             BorderThickness="1"
                             Foreground="White"
                             CaretBrush="White"
                             FontSize="22"
                             HorizontalContentAlignment="Center"
                             VerticalContentAlignment="Center"
                             TextChanged="TimeTextBox_TextChanged"/>


                    <!-- Add Alarm Button -->
                    <Button x:Name="AddAlarmButton"
                    Content="＋  Add Alarm"
                    Width="200"
                    Height="50"
                    Margin="0,20,0,0"
                    FontSize="17"
                    FontWeight="SemiBold"
                    Foreground="White"
                    Background="#663C1475"
                    BorderBrush="Pink"
                    BorderThickness="1"
                    Cursor="Hand"
                    Click="AddAlarmButton_Click"/>

                </StackPanel>

            </Border>


            <!-- ========================= -->
            <!-- ACTIVE ALARMS -->
            <!-- ========================= -->

            <Border Grid.Column="2"
                    Padding="24"
                    CornerRadius="28"
                    Background="#22000000"
                    BorderBrush="#88FFFFFF"
                    BorderThickness="1">

                <Grid>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>


                    <!-- Header -->
                    <Grid Grid.Row="0">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">

                            <TextBlock Text="Active Alarms"
                                       FontSize="26"
                                       FontWeight="SemiBold"
                                       Foreground="White"/>

                            <TextBlock Text="Your currently scheduled alarms"
                                       FontSize="13"
                                       Foreground="#CCFFFFFF"
                                       Margin="0,3,0,0"/>

                        </StackPanel>


                        <!-- Alarm Counter -->
                        <Border Grid.Column="1"
                                Width="55"
                                Height="55"
                                CornerRadius="18"
                                Background="#553C1475"
                                BorderBrush="Pink"
                                BorderThickness="1">

                            <TextBlock x:Name="AlarmCountText"
                                       Text="0"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>

                        </Border>

                    </Grid>


                    <!-- Separator -->
                    <Border Grid.Row="1"
                            Height="1"
                            Background="#55FFFFFF"
                            Margin="0,20,0,15"/>


                    <!--
                        Active alarms will be inserted here
                        The button handler dynamically populates the container (not yet stylized though)
                    -->
                    <ScrollViewer Grid.Row="2"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">

                        <StackPanel x:Name="ActiveAlarmsPanel">

                            <!-- Empty state -->
                            <TextBlock x:Name="NoAlarmsText"
                                       Text="No active alarms"
                                       FontSize="16"
                                       Foreground="#AAFFFFFF"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Margin="0,35,0,35"/>

                        </StackPanel>

                    </ScrollViewer>

                </Grid>

            </Border>

        </Grid>

    </Grid>

</Page>